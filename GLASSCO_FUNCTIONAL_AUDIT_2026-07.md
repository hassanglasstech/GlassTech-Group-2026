# Glassco/Nippon Multitenant — Functional Bug Audit (2026-07-03)

Source: whole-ERP functional/data/financial/RLS/concurrency finder sweep (11 module lenses).
**These are UNVERIFIED finder candidates** — each must be adversarially confirmed against the
cited line before fixing (some will be false positives). Scope excludes pure UI/UX (separate pass).

Counts: **P1 21** · P2 32 · P3 11 · total 64

Status legend: ⬜ open · ✅ fixed · 🟡 partial · 🔶 confirmed—needs decision · ❌ refuted

## Progress — 2026-07-03 (lean verify-inline pass, no Fable/fan-out)
- ✅ **P1-12** `ad1d88b` — per-piece sqft: delivery COGS + inventory relief were ×qty overstated on every multi-qty Glassco line.
- ✅ **P1-3** `822bb13` — cold-cache guard: approveCreditNote/voidInvoice now hydrate the ledger from cloud when the invoice GL is missing locally, so the COGS/AR reversal posts instead of silently skipping.
- ✅ **P1-11** — production_pieces upsert now stamps company (per-piece from its order), fixing RLS insert failures / invisible pieces.
- 🟡 **P1-2** — partially refuted: void already warns (warnMissingGL); improved by the 822bb13 cold-cache guard. Residual is a P2.
- 🔶 **P1-13** — CONFIRMED double inventory relief: delivery COGS credits Glass Inventory (11511) which cutting-close already relieved (Dr WIP 11513/Cr 11511), so WIP 11513 inflates forever. Correct fix (Cr WIP at delivery) is risky if a cutting session never posted GL-CUT-*; **needs Hassan's confirmation that every delivered Glassco order goes through a cutting-session close that posts GL** before changing the posting.

---

## P1 (21)

### ⬜ P1-1 — Duplicate-GL guard in handleConfirmGRN checks docType==='WE' but MIGO's GRN material GL is docType 'JV' (only its id is 
- **File:** `modules/finance/components/ThreeWayMatching.tsx:96`  ·  **module:** procurement/3-way-match
- **Why it fails:** Normal flow: post glass GRN via GoodsReceiptMIGO (grnGLService.postGRNMaterialGL writes docType:'JV', referenceId=grnId, Posted) -> open ThreeWayMatching, link the same grnRef. existingGLs filter (referenceId===grnRef && docType==='WE') finds nothing -> a second Posted entry Dr Inventory / Cr GR-IR is recorded. Inventory and GR/IR are double-counted for every PO-linked GRN. Bonus bug on line 104: grnAmount = grnForm.grnQty || selectedPO.totalAmount — grnQty is a QUANTITY (sheets/sqft) used as the PKR GL amount, so entering qty 500 posts a PKR 500 journal.
- **Fix:** Change the guard to match what MIGO actually writes: filter on referenceId===grnRef && (docType==='JV' || id.startsWith('WE-')), and never use grnQty as a money amount — use selectedPO.totalAmount (or a dedicated grnValue field).

### ⬜ P1-2 — Posted tab renders Edit and Delete buttons on POSTED documents — posted GL entries can be mutated or locally hidden with
- **File:** `modules/finance/pages/GeneralLedger.tsx:399`  ·  **module:** GeneralLedger
- **Why it fails:** The action cell (lines 397-414) is not gated by activeTab/status. On the Posted tab, Edit → handleEditParked → handleSaveDocument replaces the posted tx with new amounts; saveLedger's dirty-set sees it changed and UPSERTs the mutation to Supabase (financeService.ts:599-604) — posted financials rewritten with no audit trail. Delete → handleDeleteParked filters it from the local array, but the dirty-set never deletes cloud rows, so local TB/dashboard exclude the entry while the trial_balance RPC still includes it, and the row resurrects on next pull — books diverge between paths.
- **Fix:** Render Edit/Delete only when tx.status === 'Parked' (and only on the Parked tab); route posted-entry corrections through voidInvoice/reversal JV or softDeleteLedgerEntry once migration 089 is live.

### ⬜ P1-3 — Live AR/AP aging treats EVERY level-5 Asset/Liability account as a receivable/payable and buckets the WHOLE balance by l
- **File:** `modules/finance/pages/ReportsHub.tsx:474`  ·  **module:** Finance Reports (ReportsHub — live AR/AP Aging)
- **Why it fails:** targetAccounts = accounts.filter(type===Asset && level===5) — Cash in Hand 11112, banks 1112x, inventory 115xx, employee advances 11421 all appear as 'receivables', so Total AR ≈ total current assets. Then daysPast = days since the LAST tx touching the account (line 491) and the ENTIRE balance lands in ONE bucket (494-498): one fresh invoice/receipt on an account with 6-month-old dues moves the full balance to 'Current 0-30', hiding all 90+ exposure. This is the mounted aging report (CompanyAccounts > Reports Hub).
- **Fix:** Restrict target accounts to the AR control subtree (parents 122/1221 per deliveryInvoiceService JIT chain) and AP subtree (2111); age per open item (FIFO walk like modules/finance/components/AgingReport.tsx lines 88-113, or the ar_aging RPC), not whole-balance-by-last-activity.

### ⬜ P1-4 — Finance cache is loaded once per login for the boot-time company and never reloaded on company switch — all cache-fed fi
- **File:** `modules/finance/services/financeService.ts:221`  ·  **module:** Finance cache / multitenant
- **Why it fails:** _loadCache queries accounts/ledger with .eq('company', activeCompany()) exactly once (FinanceService.init at App.tsx:514, dep [user?.id]); appStore.setSelectedCompany (appStore.ts:16) triggers no refresh and init() is a no-op once loaded. Switch Glassco→Nippon: getAccounts()/getLedger() still return Glassco → GeneralLedger detail lines resolve to 'Unknown'/'0000', dashboardMetrics/FinanceDashboard show zeros, getTrialBalanceAsync returns [] even when the RPC works (buildRows maps totals over a 0-account stale list, line 1194). Boot race worsens it: selectedCompany defaults 'Nippon' and the role-default effect (App.tsx:556-565) may flip it AFTER _loadCache already ran.
- **Fix:** Subscribe to appStore.selectedCompany (useAppStore.subscribe) and call FinanceService.refresh() on change (same for HR/Sales caches); in getTrialBalanceAsync, fetch the account list for the requested company from Supabase when the cached list is empty/mismatched.

### ⬜ P1-5 — Vendor-Included freight and unloading labour are double-posted: they are capitalized into inventory with a Cr Cash insid
- **File:** `modules/procurement/components/inventory/GoodsReceiptMIGO.tsx:727`  ·  **module:** procurement/GRN-GL (Glassco MIGO)
- **Why it fails:** totalLandedForGL = freightPKR + craneAmount + labourNet + otherCharges regardless of freightType. grnGLService.postGRNMaterialGL credits Cash for the full landed total (Posted). Then orchestrateGRNGL (grnGLService.ts:635) ALSO posts postFreightVendorIncludedGL (Dr Vendor Payable / Cr Cash, Posted) for the same freight when freightType==='Vendor Included', and postLabourPackingPV (Dr Labour Expense gross / Cr Cash net, Parked — posted later by Finance). One physical cash payment of e.g. PKR 20,000 freight ends up as PKR 40,000 of cash credits, and labour cost is both capitalized into inventory AND expensed.
- **Fix:** Pick one treatment per charge: if capitalized via landedChargesTotal, exclude it from the separate PV (pass freight into landedChargesTotal only when freightType==='Own Expense', and drop the labour PV's Cr Cash leg or drop labour from landedChargesTotal). Add a unit test asserting total Cr Cash across all GRN entries equals actual cash outflow.

### ⬜ P1-6 — GL credit uses totalAmount (which includes Damaged/Short lines) while debits sum only condition==='OK' lines, producing 
- **File:** `modules/procurement/components/inventory/GTKStoreReceipt.tsx:324`  ·  **module:** procurement/GRN intake (GTK/Glassco local purchase)
- **Why it fails:** Receive 2 lines: OK PKR 10,000 + Damaged PKR 3,000. Direct-purchase path builds Dr 10,000 / Cr 13,000 and saves it Parked (saveLedger only asserts balance on Posted). Finance can never post it — postParkedPV throws LedgerImbalanceError — and the damaged PKR 3,000 vanishes (no vendor claim, no expense). Same skew hits settleAdvance (actualAmount=13,000 vs categoryTotals=10,000 -> imbalanced settlement JV, financeService.ts:1128-1176). The direct saveLedger path also skips the period-close gate, and accountIds `${company}-11513` etc. are never ensureAccount'd — for Glassco (this modal is mounted for Glassco at InventoryModule.tsx:355) codes 11513/11531 may not exist, so the entry references orphan accounts invisible to the trial balance.
- **Fix:** Exclude Damaged/Short amounts from totalAmount for GL/settlement (or post them to a vendor-claim/GR-IR line so the entry balances), call FinanceService.ensureAccount for each code, and post via recordTransaction instead of raw saveLedger. Also add an isPosting guard — handlePost (line 162) has no double-click protection.

### ⬜ P1-7 — MAP formula totalValue/(quantity||1) corrupts moving average whenever Nippon stock is zero or negative (which Nippon exp
- **File:** `modules/procurement/components/inventory/NipponGoodsReceipt.tsx:213`  ·  **module:** procurement/GRN intake (Nippon) — MAP
- **Why it fails:** Nippon item oversold to qty -10 (saveStore's negative-stock guard deliberately skips Nippon, inventoryService.ts:426). Receive 10 pcs @ PKR 100: quantity becomes 0, so (quantity||1) divides by 1 and MAP = totalValue (the ENTIRE batch value, e.g. 1,000) instead of ~100 per unit. Next sale's COGS (buildNipponTradingCOGSPlan uses qty x MAP) is overstated 10x. Negative-to-positive receipts drift similarly (totalValue never went negative with qty). Same formula in the manual path at line 299.
- **Fix:** When pre-receipt quantity <= 0, reset the batch: MAP = landed unit price of this receipt and totalValue = max(0,newQty) * MAP, instead of accumulating totalValue across a negative-qty period; guard division by newQty <= 0 explicitly.

### 🔶 P1-8 — Delivery COGS credits Glass Inventory (11511) which was ALREADY credited at cutting-session close (Dr WIP 11513 / Cr 115
- **File:** `modules/procurement/services/glasscoGLDelivery.ts:100`  ·  **module:** procurement/glasscoGL (delivery COGS)
- **Why it fails:** Invoicing requires the cutting session to be closed first (deliveryInvoiceService.ts:340 error message), and cutting close posts Dr WIP 11513 / Cr GlassInv 11511 for the full sheet value (glasscoGLCutting.ts:72-81 via consume_glass_stock). At delivery, buildDeliveryCOGSPlan/postDeliveryCOGS post Dr COGS 5111 / Cr GlassInv 11511 AGAIN (accs.glassInv, not accs.wip). Every delivered glass order: 11511 goes negative by the piece value, 11513 accumulates the sheet value forever -> balance sheet inventory wrong and compounding, even though each entry individually balances.
- **Fix:** At delivery, credit accs.wip (11513 WIP — Glass in Process) instead of accs.glassInv for the raw-glass portion (mirroring how labour is closed from wipLabour). Reconcile historic entries with a one-time JV moving the accumulated 11513 balance against 11511.
- **STATUS: 🔶 CONFIRMED double inventory relief (WIP 11513 never closed) — NEEDS operational decision, not auto-fixed (see progress note)**

### ✅ P1-9 — getPieceCostData uses item.totalSqFt (LINE total, already multiplied by qty) as the sqft of EACH piece, so delivery COGS
- **File:** `modules/procurement/services/glasscoGLHelpers.ts:146`  ·  **module:** procurement/glasscoGL (delivery COGS + tempering AP)
- **Why it fails:** JobRegistryView.tsx:74 creates one piece PER UNIT (qty pieces per item, all with the same itemIndex), and GlasscoUtils.calculateLineItemTotal:126 computes totalSqFt = w*h/144 * qty. Repro: line with qty=4, 3x4ft pieces (totalSqFt=48). buildDeliveryCOGSPlan (glasscoGLDelivery.ts:61-69) iterates the 4 pieces, each returning sqft=48 -> rawGlassCOGS = 4 x 48 x MAP = 192 sqft instead of 48 -> COGS and Cr Glass Inventory 4x overstated. Same in postTemperingInwardGL (glasscoGLService.ts:111 'item?.totalSqFt || piece.sqft') -> vendor AP = 4 x line-sqft x rate, vendor liability 4x the real bill.
- **Fix:** Compute per-piece sqft = item.totalSqFt / Math.max(1, Math.round(item.qty || 1)) in getPieceCostData and in postTemperingInwardGL (or persist a per-piece sqft on the piece at generation and use piece.sqft). Add a SIT test with qty>1.
- **STATUS: ✅ FIXED ad1d88b — per-piece sqft (COGS + inv relief were ×qty)**

### ⬜ P1-10 — getStoreAsync (and getStockLedgerAsync:542, getGRNSheetEntriesAsync:707, getCuttingSessionsAsync:863) overwrite the shar
- **File:** `modules/procurement/services/inventoryService.ts:409`  ·  **module:** inventory/two-tier cache
- **Why it fails:** Post a GRN while the store_items/stock_ledger upsert fails (offline or RLS error — _inventoryUpsert only toasts, there is NO retry queue). Then open Inventory (InventoryModule.refreshData calls getStoreAsync on every tab change): the cloud read succeeds and safeSave overwrites gtk_erp_store with pre-GRN quantities — the received stock and its ledger rows are permanently lost while the GL entry survives (FinanceService has its own retry queue) -> stock vs GL divergence. Also, because the write is company-filtered but the key is shared, switching company in the multitenant sidebar erases the other company's cached rows.
- **Fix:** Merge instead of replace: on pull, replace only rows of the active company (keep other companies' rows), and skip overwrite when a dirty/unsynced flag exists for the table; add a retry queue to _inventoryUpsert like financeService.flushRetryQueue.

### ✅ P1-11 — saveProductionPieces upserts pieces WITHOUT the company column under live strict company-RLS — pieces either never reach
- **File:** `modules/production/services/productionService.ts:244`  ·  **module:** production/sales
- **Why it fails:** The push mapper (lines 242-254) and SyncService's TABLE_PUSH.production_pieces (SyncService.ts:238-245) omit `company`; the live table has company TEXT NOT NULL DEFAULT '' (migration 038) and 086/054 strict RLS enforces WITH CHECK company = ANY(auth_user_companies()) unless super-admin. Approve a Glassco order as factory_manager/glassco_cutter → the fire-and-forget upsert (line 256, only console.warn on error, no markDirty retry) is rejected by RLS → pieces exist in this browser's localStorage only. As super-admin the insert succeeds with company='' → getProductionPiecesPage's .eq('company','Glassco') (line 124/131) returns none and non-super users can never SELECT them. Either way, freshly approved orders' pieces don't appear in production views on any other device/user.
- **Fix:** Stamp company on every pushed piece row in BOTH mappers (derive from the order's company, or from the GLS/GTK order-id pattern as a fallback), queue a retry (SyncService.markDirty('production_pieces')) when the upsert errors, and run a one-off backfill: UPDATE production_pieces SET company='Glassco' WHERE company='' AND order_id ILIKE '%GLS%'.
- **STATUS: ✅ FIXED — stamp company per-piece on production_pieces upsert (RLS/data-loss)**

### ⬜ P1-12 — ProductionService.savePurchaseOrders is safeSave-only (no Supabase push, no markDirty), so every 3-way-match state chang
- **File:** `modules/production/services/productionService.ts:107`  ·  **module:** procurement/purchase-orders sync
- **Why it fails:** ThreeWayMatching.tsx (lines 129, 204, 242, 283) and GoodsReceiptMIGO's PO update all call ProductionService.savePurchaseOrders. SyncService.pullTable('purchase_orders') runs on every app start (fetchFromCloud) and overwrites gtk_erp_purchase_orders wholesale with cloud rows that never received these fields (toRowMappers.purchase_orders at SyncService.ts:379 only pushes 8 flat fields and is only used by markDirty, which is never called). After a restart the PO reverts to pre-match status while the AP GL entries persist -> user re-registers the invoice -> duplicate Dr GR-IR / Cr AP and potential double vendor payment.
- **Fix:** Route PO writes through one synced saver: make ProductionService.savePurchaseOrders push to Supabase (mirror InventoryService.savePurchaseOrders) and extend the SyncService push/pull mappers to round-trip grn_ref, grn_date, vendor_invoice_no/date/amount, match_status, approval_history, ap_invoice_id.

### ⬜ P1-13 — Two independent serial counters ('GT-QUT' and 'GT-SO', both seeded 2523) mint numbers into the same GT-SO-GLS-mmyy-#### 
- **File:** `modules/sales/companies/glassco/useGlasscoQuotations.ts:186`  ·  **module:** sales/glassco
- **Why it fails:** Path 1: 'save' allocates allocateSerial(company,'GT-QUT',year,2523) → GT-QUT-GLS-0726-2523; approving later just swaps the prefix (lines 190-192, 203) → GT-SO-GLS-0726-2523. Path 2: direct 'approve' of a new quote allocates from the SEPARATE 'GT-SO' counter (line 186-188), whose first value is also 2523 → GT-SO-GLS-0726-2523. Both counters advance independently, so the same seq+mmyy recurs; saveQuotations upserts onConflict:'id' (asyncSalesService.ts:435) → the second order silently REPLACES the first (different client/items), production pieces and invoices then attach to the surviving row. The legacy AppService used one shared MASTER series for QUT+SO precisely to prevent this; the allocate_serial migration split them.
- **Fix:** Allocate QUT and SO numbers from ONE doc_type series (e.g. always allocateSerial(company,'GT-MASTER',...) for both 'save' and 'approve'), or re-allocate a fresh 'GT-SO' number on QUT→SO conversion instead of reusing the QUT number. Also add a unique guard: before upsert, verify no existing row already owns the freshly minted id.

### ⬜ P1-14 — Bootstrap store rows created at Nippon SO approval set movingAveragePrice = item.pricePerUnit (the SELLING price), so th
- **File:** `modules/sales/companies/nippon/useNipponQuotations.ts:399`  ·  **module:** sales/nippon (trading COGS)
- **Why it fails:** Sell an uncounted item (no store row): approval pushes a row with quantity -qty and MAP = sale price. buildNipponTradingCOGSPlan (deliveryInvoiceService.ts:133-139) then resolves that row and uses unitCost = si.movingAveragePrice -> COGS = qty x SALE price = revenue. P&L shows 0 gross profit for the line and 11514 inventory goes negative by the full sale value. The intended 'no stock match -> loud NO-COGS warning' path is bypassed because a (bogus) store row now exists.
- **Fix:** Create the bootstrap row with movingAveragePrice: 0 (and let the existing unmatched/zero-cost warning fire), or seed MAP from the product master's cost/purchase price — never from item.pricePerUnit.

### ⬜ P1-15 — getQuotations' cloud-success path overwrites the shared multi-company local cache with ONLY the active company's rows, w
- **File:** `modules/sales/services/asyncSalesService.ts:374`  ·  **module:** sales
- **Why it fails:** safeSave(KEYS.QUOTATIONS, mapped) replaces the whole gtk_erp_quotations key with rows for activeCompany() only. Repro A (data loss): a Glassco quote save fails cloud-push (queued via _queueRetry) → user switches sidebar to Nippon → NipponQuotationManager's refreshData calls getQuotations → Nippon cloud rows overwrite the key → the dirty Glassco row is erased before pushTable reads the key → write never reaches Supabase, silently lost. Repro B (hidden rows): components/SalesOrders.tsx:126 reads the SYNC local cache (SalesService.getQuotations) and filters q.company===company — after visiting Nippon quotations the cache holds only Nippon rows, so Glassco's Sales Orders tab shows 0 approved orders until some async Glassco read re-truncates the cache the other way. getClients (line 134), getInvoices (533), getCreditNotes (718) etc. share the same pattern.
- **Fix:** Replace the blanket safeSave with a company-scoped merge: keep all cached rows whose company !== activeCompany(), replace only the active company's slice (and never drop rows that are in the pending-sync queue). Apply the same to getClients/getInvoices/getCreditNotes/getCustomerComplaints.

### ✅ P1-16 — cogsReversalPending mis-flag: reverseDeliveryCOGS silently returns (console.warn, no throw) when GL-COGS-<invoiceId> is 
- **File:** `modules\procurement\services\glasscoGLDelivery.ts:347`  ·  **module:** procurement/glasscoGLDelivery (COGS reversal)
- **Why it fails:** The P1-08 design in creditNoteService.ts:349-369 only sets cogsReversalPending when reverseDeliveryCOGS THROWS. But its most likely failure mode is the non-throwing one: FinanceService.getLedger() is local-only, so approving a CN on any device that never hydrated the delivery-day ledger (second machine, cleared storage, partial pull) finds no cogsTx and returns at line 349. Result: revenue/AR reversed, COGS NOT reversed, cogsReversalPending stays false, finance dashboard shows nothing. The flag also stays false when recordTransaction succeeds but the inventory restoration block silently catches its own errors (line 410) - store value and GL diverge quietly.
- **Fix:** Make reverseDeliveryCOGS return a discriminated result ({ reversed: true } | { reversed: false, reason: 'cogs_tx_not_found' | ... }) instead of void, checking the CLOUD ledger for GL-COGS-<invoiceId> before deciding nothing exists. In approveCreditNote/voidInvoice, set cogsReversalPending + toast whenever reversed === false and the invoice is expected to have delivery COGS (e.g. cloud lookup hit or invoice has pieces/trading COGS). Distinguish 'no COGS ever posted' (legit skip) from 'not found locally' (must flag).
- **STATUS: ✅ FIXED 822bb13 — cold-cache guard hydrates GL-COGS-* before reversal**

### ⬜ P1-17 — credit_note_atomic writes a CLIENT-computed absolute invoice balance (invoice_new_balance) with no server-side re-valida
- **File:** `modules\sales\services\creditNoteService.ts:311`  ·  **module:** sales/creditNote
- **Why it fails:** Invoice balance 1000. Device A posts a receipt of 600 (cloud balance -> 400). Device B, whose UI loaded before the receipt, approves a CN of 500: client computes newBalance = 1000-500 = 500 and the RPC (090 lines 99-103) blindly sets balance=500 and posts a 500 AR credit. Cloud now says balance 500 but true AR position is 400-500 = -100: AR sub-ledger over-credited past zero and invoices.balance overwrites the fresher receipt value. Same failure with two concurrent CNs on one invoice (both pass the issue-time check against the same stale balance; last writer's absolute value wins while GL reduces AR by the SUM). approveCreditNote also never re-checks amount <= balance at approval time - only issueCreditNote checks, and the balance can shrink between issue and approve.
- **Fix:** In credit_note_atomic: lock the invoice (SELECT balance ... FOR UPDATE), pass the CN amount instead of an absolute balance, RAISE if amount > balance + 0.01, then SET balance = GREATEST(0, balance - amount) and derive status server-side. Client mirrors the RPC's returned balance instead of its own computation. Until 090 is applied, approveCreditNote should re-fetch the live invoice (AsyncSalesService.getInvoices or a single .eq('id') read) and re-assert amount <= balance before posting.

### 🟡 P1-18 — voidInvoice builds the GL reversal from the LOCAL ledger cache only (FinanceService.getLedger); on a cold cache it voids
- **File:** `modules\sales\services\creditNoteService.ts:431`  ·  **module:** sales/creditNote
- **Why it fails:** FinanceService.getLedger() (financeService.ts:510) reads _cache/localStorage only. On a fresh device, after clearing site data, or when the ledger pull hasn't hydrated that old transaction, origTx is null even though GL-<invId> EXISTS in the cloud ledger table. voidInvoice then sends reversal_ledger_row: null, the RPC (or legacy path) happily marks the invoice Voided + reverts the quotation, and the only signal is a toast (warnMissingGL). Revenue, GST Payable and AR stay on the books forever unless a human notices and posts a manual JV. Contrast: approveCreditNote hard-fails in the same situation (line 234), which is the safe behaviour.
- **Fix:** Before concluding the GL is missing, do a cloud lookup: await supabase.from('ledger').select('*').eq('id', invoice.glTxId).eq('company', company).maybeSingle(), fall back to the local cache only on network failure. Better: have void_invoice_atomic build the reversal server-side from the ledger row it can see (SELECT details FROM ledger WHERE id = ...), so the void is only reversal-free when the GL genuinely does not exist - and in that case require an explicit force flag from the operator instead of a toast.
- **STATUS: 🟡 PARTIAL — warnMissingGL already signals; cold-cache guard added 822bb13; residual = P2 (auto-fetch)**

### ⬜ P1-19 — Boot-time cloud pull overwrites localStorage BEFORE pending offline quotation writes are pushed, permanently wiping unsy
- **File:** `src/services/SyncService.ts:1663`  ·  **module:** sync/sales
- **Why it fails:** Save a quotation while offline: saveQuotations merges it into gtk_erp_quotations and _queueRetry('quotations') persists a pending entry (gtk_erp_pending_sync). Close the app, reopen while online: App.tsx init (App.tsx:487-502) runs SyncService.fetchCritical()/fetchFromCloud() which call pullTable → localStorage.setItem(localKey, JSON.stringify(cloudData)) — a FULL overwrite that does not contain the unsynced row. pushPending() is never called at boot (only on the 'online' event and a 5-min interval; flushOfflineQueue() at App.tsx:502 flushes a different queue). When pushPending finally runs, pushTable (SyncService.ts:1505) reads the already-overwritten key — the offline quote is gone from local AND cloud, with zero error surfaced.
- **Fix:** In App.tsx init, await SyncService.pushPending() (flushing gtk_erp_pending_sync) BEFORE fetchCritical()/fetchFromCloud(); additionally make pullTable skip (or merge-by-id instead of overwrite) any table that currently has a pending dirty entry, so a pull can never clobber locally-dirty rows.

### ⬜ P1-20 — post_invoice_atomic merges the quotation patch (status='Invoiced', wastage items) ONLY into the data JSONB, never the fl
- **File:** `supabase/migrations/042_atomic_rpcs.sql:188`  ·  **module:** db/rpc + sales sync (quotation status)
- **Why it fails:** quotations is hybrid (032 added flat status/items; index idx_quotations_status). RPC step 3 does SET data = data || patch. asyncSalesService.getQuotations:355-356 rebuilds with 'status: r.status ?? base.status' and 'items: r.items (if non-empty)' then safeSave(KEYS.QUOTATIONS, mapped) — the cloud flat status ('Approved') wins over data.status ('Invoiced') and clobbers the local mirror written in deliveryInvoiceService.ts:634-639. Order reappears as invoiceable; if the invoices cache is cold or a second device is used (guard at deliveryInvoiceService.ts:237 is local-cache only, RPC dedupes by invoice id only, and each attempt allocates a fresh serial), the same order can be invoiced twice -> double revenue + double COGS.
- **Fix:** In post_invoice_atomic step 3 also update the flat columns: SET status = COALESCE(patch->>'status', status), items = COALESCE(patch->'items', items), data = data || patch. Additionally add a partial unique index ON invoices(order_id) WHERE status <> 'Voided' and a duplicate-order check inside the RPC.

### ⬜ P1-21 — trial_balance / ar_aging / attendance_summary are SECURITY DEFINER, GRANTed to anon+authenticated, with zero company-aut
- **File:** `supabase/migrations/088_finance_aggregation_rpcs.sql:70`  ·  **module:** 088 aggregation RPCs
- **Why it fails:** 086 strict RLS is live, but SECURITY DEFINER runs as the function owner and bypasses RLS. GRANT EXECUTE ... TO authenticated, anon (lines 70/119/156) means an UNAUTHENTICATED caller can POST /rest/v1/rpc/trial_balance {p_company:'Glassco'} with only the public anon key and receive per-account Dr/Cr totals; any company-A user can pull company-B's trial balance, AR totals, and per-employee attendance. The staged migration ships a cross-company + public financial-data leak the day the founder applies it.
- **Fix:** Remove the anon grant; inside each function require p_company = ANY(auth_user_companies()) OR auth_user_is_super() (raise exception otherwise), or make them SECURITY INVOKER so 086 RLS applies to the underlying scans.

---

## P2 (32)

### ⬜ P2-22 — isTaxEnabled returns false whenever the erp_config fetch errors (offline, RLS, timeout), so an invoice the operator expl
- **File:** `modules/admin/services/taxSettingsService.ts:98`  ·  **module:** sales/invoicing (GST gate)
- **Why it fails:** loadTaxSettings returns {error} on any Supabase error; isTaxEnabled maps that to false. deliveryInvoiceService.ts:288-290 then forces effectiveGstPercent = 0 and posts the invoice GST-free with status Posted. For a company with tax enabled, one transient network blip -> understated Sales Tax Payable and a legally wrong tax invoice, discoverable only by manual audit.
- **Fix:** Distinguish 'settings unavailable' from 'disabled': have isTaxEnabled surface the error, and in generateDeliveryInvoice throw (block the post) when gstPercent > 0 but the tax settings cannot be read; optionally fall back to a locally cached last-known settings value.

### ⬜ P2-23 — Factory Desk finance tab hardcodes company 'Glassco', includes Parked/Draft entries, uses the same nonexistent 123/121/2
- **File:** `modules/factory/components/mis/FinancialStatementsMobile.tsx:86`  ·  **module:** Factory MIS statements (mounted)
- **Why it fails:** accounts/ledger filter a.company === 'Glassco' from the FinanceService cache, which is loaded for activeCompany() at boot — for a Factory-company session the cache holds Factory rows so every figure is silently 0; when the cache does hold Glassco, statements include Parked/Draft entries (no status filter, line 87), cash card sums prefix '123' (nothing) and inventory '121' (nothing), and line 121 invents COGS with a hardcoded 0.65 multiplier presented as Gross Profit to management.
- **Fix:** Take company from appStore.selectedCompany (or a prop), filter status==='Posted', use real COA prefixes (111/115/122/2111), and compute gross profit from actual 51xx COGS postings instead of the 0.65 heuristic.

### ⬜ P2-24 — Overriding a Mismatch sets the PO to 'Matched' without ever posting the MIRO AP entry (only a clean 3-Way posts Dr GR-IR
- **File:** `modules/finance/components/ThreeWayMatching.tsx:306`  ·  **module:** procurement/3-way-match override
- **Why it fails:** Invoice differs from PO by >2% -> matchSt='Mismatch' -> handleRegisterInvoice skips the GL block entirely -> user clicks Override -> status='Matched' -> approval -> handlePostPayment posts Dr Payable 21111 / Cr Bank for selectedPO.totalAmount. Net effect: AP goes debit (negative payable), GR/IR credit balance from the GRN stays open forever, and the vendor is paid the PO total rather than the (different) invoice amount — the exact 3-way-match bypass the tolerance was supposed to prevent.
- **Fix:** On override, post the deferred MIRO entry (Dr GR-IR at GRN value / Cr AP at approved invoice amount, difference to a price-variance account) before allowing the payment step, and make handlePostPayment pay vendorInvoiceAmount, not totalAmount.

### ⬜ P2-25 — P&L/BS/CF include Parked and Draft ledger entries — no status==='Posted' filter (unlike every other report).
- **File:** `modules/finance/pages/FinancialStatements.tsx:10`  ·  **module:** Financial Statements (desktop, orphaned)
- **Why it fails:** getLedger().filter(t => t.company === company) omits the status filter that TrialBalance fallback (financeService.ts:1211), components/AgingReport (line 48) and dashboardMetrics (line 184) all apply. Park a 1M expense JV → the printable IFRS-labelled P&L immediately shows it as expense; deleting the parked doc changes the statements. NOTE: this component is currently orphaned in the multitenant app (not imported anywhere) — flagging before it is re-wired, since the prompt-level bug is identical in the mounted FinancialStatementsMobile (separate finding).
- **Fix:** Filter t.status === 'Posted' (and add the filter before any re-mount of this page).

### ⬜ P2-26 — Cash-flow classification uses COA prefixes (cash 123, inventory 121, payables 22, PPE 11) that do not exist in the Glass
- **File:** `modules/finance/pages/FinancialStatements.tsx:72`  ·  **module:** Financial Statements (desktop, orphaned) — Cash Flow
- **Why it fails:** Real codes are cash 111xx, inventory 115xx, AP 2111x (coa.glassco.ts:6/40/101), AR at 122/1221 via the JIT chain (deliveryInvoiceService.ts:352-357). With this data: no account starts '123' → Closing Cash = 0; every 11xxx account (cash, banks, inventory, advances) falls into the code.startsWith('11') branch → giant fictitious 'Purchase of Fixed Assets' outflow; no '22' accounts → payables change = 0 while trade payables (2111) are booked as long-term financing inflow. The statement is structurally wrong for every company.
- **Fix:** Classify from the actual COA: cash '111', receivables '122' (and '112'), inventory '115', trade payables '2111', PPE '12' fixed-asset subtree — or walk the account tree by parent instead of raw prefixes.

### ⬜ P2-27 — Editing/posting a document that exists on the server page but not in the local finance cache is a silent no-op that stil
- **File:** `modules/finance/pages/GeneralLedger.tsx:214`  ·  **module:** GeneralLedger
- **Why it fails:** The grid rows come from useSupabasePage (server), but the save path maps over FinanceService.getLedger() (boot-company localStorage cache). Cold cache / other-device doc / post-switch company: updatedTxs.map(t => t.id === editingDocId ? tx : t) matches nothing, saveLedger persists no change, postParkedPV returns null (unchecked, line 194) — yet the UI alerts 'Success: PV Posted'. The user believes the voucher is posted; neither localStorage nor Supabase changed. Bonus corruption: rows from useSupabasePage are snake_case (doc_type/reference_id), so handleEditParked seeds formData.docType/referenceId as undefined and a successful edit blanks those fields locally.
- **Fix:** In handleSaveDocument, if editingDocId is not found in the cache array, append the edited tx instead of mapping (or refuse with an error); check postParkedPV's return before alerting; map doc_type→docType, doc_date→docDate, reference_id→referenceId when seeding the edit form.

### ⬜ P2-28 — Sales Analysis and GST Return select nonexistent invoices columns (grand_total, gst) with the error silently discarded, 
- **File:** `modules/finance/pages/ReportsHub.tsx:731`  ·  **module:** Finance Reports (ReportsHub — Sales Analysis / GST Return)
- **Why it fails:** Migration 055's own hotfix comment states the live invoices table has total_amount NOT grand_total; selecting an unknown column makes PostgREST return 42703 and `const { data } = await ...` (no error check, lines 729/886) yields data=null → both reports render permanently empty ('No invoices/GST in this period') with no error surfaced — a compliance report silently blank. Additionally .not('status','in','("cancelled","draft")') never matches the capitalized 'Cancelled'/'Draft' vocabulary (037 CHECK) and 'Voided' is not excluded at all, so if the columns are ever added, GST payable would include voided invoices. Same grand_total bug also in modules/sales/pages/ProjectProfitability.tsx:58.
- **Fix:** Select total_amount (and derive GST from the data jsonb or a real column), check and surface query errors, and exclude ('Voided','Cancelled','Draft') with correct casing.

### ⬜ P2-29 — ReportsHub fetches once on mount; switching sidebar company or toggling Group Mode relabels the header immediately while
- **File:** `modules/finance/pages/ReportsHub.tsx:1057`  ·  **module:** Finance Reports (ReportsHub shell)
- **Why it fails:** useEffect(() => { load(); }, []) has no deps. CompanyAccounts re-renders ReportsHub with the new company prop after a sidebar switch (AccountsModule.tsx:6), targetCompanies changes, but no reload runs — header shows 'Nippon · Updated 12:01' (or 'All Companies — Group Consolidated') over Glassco's trial balance/P&L until the user happens to press Refresh/Run Report. Cross-company numbers presented under the wrong label.
- **Fix:** useEffect(() => { load(); }, [company, groupMode]) (keep dates manual behind Run Report), or clear accounts/ledger state when company/groupMode changes so stale data can't render under the new label.

### ⬜ P2-30 — getARAgingAsync JS fallback filters status === 'Void' but the app writes 'Voided' — voided invoices inflate AR aging buc
- **File:** `modules/finance/services/financeService.ts:1264`  ·  **module:** AR Aging fallback
- **Why it fails:** The SQL RPC was fixed to 'Voided' (088:107) but the fallback still checks String(raw.status) === 'Void', which never matches (creditNoteService.ts:454 and 090 write 'Voided'; 037 CHECK has no 'Void'). 088 is staged/unapplied, so the RPC 404s and this fallback IS the production path: any voided invoice whose balance column is NULL (bal falls back to total_amount − received_amount > 0) or pre-fix stale balance lands in the buckets and total. Different instance from the already-fixed SQL bug.
- **Fix:** Change the check to exclude 'Voided' (and ideally 'Draft'/'Cancelled'): if (['Voided','Draft','Cancelled'].includes(String(raw.status ?? ''))) continue;

### ⬜ P2-31 — ensureAccount dedupes by company+code, and every client AR account is created with the same code '12210' — all clients' 
- **File:** `modules/finance/services/financeService.ts:907`  ·  **module:** finance (per-client AR accounts)
- **Why it fails:** deliveryInvoiceService.ts:354-358 calls ensureAccount(company, '<CLIENT — PROJECT>', 5, arControl.id, 'Asset', '12210'). First invoice creates id 'Nippon-12210' named 'CLIENT A'. Second client's call matches (a.company===company && a.code==='12210') and returns CLIENT A's account -> Client B's AR debit posts to the account labelled CLIENT A. Per-client GL statements/subledger are meaningless (total AR is right, attribution is wrong); receipts credit the same shared account.
- **Fix:** Give each client a unique account code/id, e.g. code `12210-${clientId}` (id `${company}-12210-${clientId}`) under the 1221 Customers Control parent, or key ensureAccount lookups on code+name for level-5 subaccounts.

### ⬜ P2-32 — The GRN 'rollback' only restores localStorage: the fire-and-forget Supabase upserts from steps 1-2 have already sent the
- **File:** `modules/procurement/components/inventory/GoodsReceiptMIGO.tsx:746`  ·  **module:** procurement/GRN atomicity (Glassco MIGO)
- **Why it fails:** saveStore/saveStockLedger fire async upserts immediately. If orchestrateGRNGL returns 0 (or throws), the catch re-saves snapshots — but (a) the rollback store_items upsert can complete BEFORE the original one (no ordering), leaving post-GRN quantities in the cloud while local shows rolled-back values, and (b) saveStockLedger(snapshotLedger) is an upsert, so the already-pushed MAT-GRN-xxx movement rows remain in Supabase forever -> stock ledger shows a 101 receipt for a GRN whose stock and GL were rolled back, breaking GRN-GL reconciliation.
- **Fix:** Make GRN posting atomic server-side: a post_grn_atomic RPC (insert stock_ledger rows + update store_items + validate GL in one transaction), or at minimum sequence the client writes (await the upserts, and on rollback issue explicit DELETEs for the new ledger row ids).

### ⬜ P2-33 — Debit note GL credits Inventory at user-entered rate x qty while stock totalValue is reduced at current MAP with Math.ma
- **File:** `modules/procurement/components/inventory/PurchaseReturnModule.tsx:250`  ·  **module:** procurement/purchase returns
- **Why it fails:** Item on hand: 5 pcs, MAP 100 (totalValue 500). User returns 8 pcs @ rate 120 (rate is editable, defaults to MAP but can be changed): GL posts Dr AP / Cr Inventory 960, but stock clamps to qty 0 / totalValue 0 — inventory GL is now 460 lower than the subledger movement, silently. There is no check that line.quantity <= on-hand and no check that ratePerUnit == MAP, and no stock_ledger (movement 122) entry is written for the return at all, so the qty change has no movement audit trail.
- **Fix:** Validate line.quantity <= unrestrictedQty before posting, value the GL credit at qty x current MAP (price difference to vendor claim/price-variance), and append a stock_ledger reversal entry per line.

### ⬜ P2-34 — Tempering inward debits WIP in GL but adds the vendor charge to RAW-sheet store totalValue/MAP (Step 3) — the raw-materi
- **File:** `modules/procurement/services/glasscoGLService.ts:262`  ·  **module:** procurement/glasscoGL (tempering inward)
- **Why it fails:** GL: Dr WIP 11513 / Cr AP 22113. Store: matched RAW store item's totalValue += charge and MAP recalculated over remaining raw qty (lines 262-283) — sheets still on the rack absorb the tempering cost of pieces already cut. At delivery, COGS = inflated MAP x sqft (tempering embedded) while 11513 still holds the tempering debit forever -> tempering cost hits P&L via MAP AND stays on the balance sheet in WIP; store value no longer ties to the Glass Inventory GL account.
- **Fix:** Remove the Step-3 raw-MAP update. Keep tempering cost in WIP and close it at delivery (Dr COGS — services / Cr WIP 11513 for the piece's tempering charge), or carry a per-piece service cost in serviceLog so the existing labour-close path picks it up.

### ⬜ P2-35 — The gated GRN GL service (assertGRNQAMatch QA gate + assertThreeWayMatch) is dead code in the multitenant app — GoodsRec
- **File:** `modules/procurement/services/grnService.ts:539`  ·  **module:** procurement/GRN GL gates (dead code)
- **Why it fails:** GoodsReceiptMIGO.tsx:16 imports orchestrateGRNGL from grnGLService (sync, no poId/vendorInvoiceAmount params, no inspection_lots check). grnService.ts's orchestrateGRNGL — the version that awaits assertGRNQAMatch and assertThreeWayMatch — has zero call sites (grep confirms). A GRN whose supplied OK/defective values disagree with inspection lots, or whose value diverges from the PO, posts inventory GL unchallenged; the only remaining 3-way check is the 2% UI tolerance in ThreeWayMatching, which is bypassable (see override finding).
- **Fix:** Port the standalone fix: have GoodsReceiptMIGO call grnService.orchestrateGRNGL (or add the two asserts to grnGLService.orchestrateGRNGL, passing poId + vendorInvoiceAmount when a PO is linked), then delete the losing duplicate file so the two orchestrators cannot drift again.

### ⬜ P2-36 — Nippon (and generic) quotation delete is a no-op: saving the filtered array never removes the row from localStorage (mer
- **File:** `modules/sales/companies/nippon/useNipponQuotations.ts:427`  ·  **module:** sales/nippon
- **Why it fails:** handleDelete does saveQuotations(all.filter(x => x.id !== id)). asyncSalesService.saveQuotations → _mergeIntoLocal (lines 69-78) starts from EXISTING local rows and overlays incoming — a row absent from incoming is kept, so local still has it; the cloud call is an upsert of the remaining rows, which never deletes the target row. Result: toast says 'Quotation deleted', refreshData re-reads the cloud and the quote is still listed. Identical bug in the generic hook: modules/sales/pages/useQuotations.ts:221.
- **Fix:** Call AsyncSalesService.deleteQuotation(id) (per-row cloud+local delete, as useGlasscoQuotations.handleDeleteQuotation already does) in both useNipponQuotations.handleDelete and useQuotations.handleDelete.

### ⬜ P2-37 — Approve seeds a brand-new stock row's movingAveragePrice with the SELLING price — Nippon COGS at delivery (qty × MAP) th
- **File:** `modules/sales/companies/nippon/useNipponQuotations.ts:399`  ·  **module:** sales/nippon
- **Why it fails:** For a line whose product isn't matched in the store (line 387-402), the negative bootstrap row is created with movingAveragePrice: Number(item.pricePerUnit) — the customer sale rate, not cost. buildNipponTradingCOGSPlan computes COGS from store MAP, so invoicing that item posts COGS 5114 = sale value → Dr COGS equals Cr revenue 4120, gross profit 0 and inventory 11514 credited at inflated value. A later GRN blends MAP from (negative qty × wrong MAP), corrupting valuation further.
- **Fix:** Seed the bootstrap row with matched?.costPrice ?? 0 (never pricePerUnit), and have the GRN/stock-take path establish MAP; flag zero-MAP items in the COGS plan so the invoice warns instead of posting sale-price COGS.

### ⬜ P2-38 — Super-admin 'PERMANENT DELETE' of a sales order deletes nothing in Supabase — the order and its production pieces resurr
- **File:** `modules/sales/components/SalesOrders.tsx:222`  ·  **module:** sales/orders
- **Why it fails:** Step 3 uses SalesService.saveQuotations(allQuos.filter(q => q.id !== order.id)) (salesService.ts:54-57 = local safeSave + AsyncSalesService.saveQuotations UPSERT — no cloud DELETE), and step 1 'deletes' pieces via ProductionService.saveProductionPieces(remainingPieces), whose cloud path is also upsert-only (productionService.ts:256). Cloud rows for the order and its pieces remain untouched; the next fetchCritical()/fetchFromCloud() pull overwrites localStorage with the cloud copy and the 'permanently deleted' order + pieces reappear, contradicting the confirmation dialog and any downstream reporting done in the interim.
- **Fix:** Use AsyncSalesService.deleteQuotation(order.id) for the quote, and add a real cloud delete for pieces (supabase.from('production_pieces').delete().eq('order_id', orderRef)) plus tempering dispatches, before pruning local caches.

### ⬜ P2-39 — Generic approve flow still has the pre-Phase-2 hazards fixed only in the Glassco hook: pieces saved BEFORE the quote per
- **File:** `modules/sales/pages/useQuotations.ts:210`  ·  **module:** sales/generic (GTK/GTI)
- **Why it fails:** (a) Line 210 writes production pieces (unawaited async) before line 213 persists the quote; saveQuotations re-validates EVERY row of the re-passed table, so a single legacy row with discount > subtotal throws → handleSave has no try/catch → unhandled rejection, no toast, quote never saved, but ghost pieces for the phantom SO already exist. (b) Line 206 filters pieces by !p.id.startsWith(`${numericPart}/`) where numericPart is only the trailing serial — a different month's order sharing the trailing number gets its pieces silently dropped from the full-overwrite save (the P1-25 collision fix was applied only to useGlasscoQuotations). (c) Line 168/183 uses AppService.generateSequenceID from a localStorage snapshot — two tabs/devices produce the same QT/SO id and the upsert overwrites one quote with the other.
- **Fix:** Port the Glassco-hook fixes into useQuotations before GTK/GTI go live: persist the quote first (per-row save [finalQuo]) inside try/catch, scope piece replacement by p.orderId === orderNo, use allocateSerial() instead of generateSequenceID, and await saveProductionPieces with rollback.

### ⬜ P2-40 — When the cloud legitimately has ZERO quotations for the active company, getQuotations falls back to the FULL unfiltered 
- **File:** `modules/sales/services/asyncSalesService.ts:377`  ·  **module:** sales
- **Why it fails:** At Nippon go-live (0 cloud quotes), getQuotations('Nippon') returns safeParse(KEYS.QUOTATIONS) containing cached Glassco rows. useNipponQuotations.handleSave (line 330 + 356) then calls saveQuotations([...all.filter(x=>x.id!==id), finalQuo]): (a) the SAL-1 loop (asyncSalesService.ts:387-396) validates the STALE Glassco rows — one legacy row with discountAmount > subtotal throws and blocks the first Nippon quote from ever saving; (b) if it passes, the upsert rewrites every stale Glassco row to the cloud with a fresh updated_at — resurrecting quotes deleted on another device and overwriting newer cloud edits with stale local copies. Same exposure in the generic hook (useQuotations.ts:166/213).
- **Fix:** Scope the fallback (and the save): in the hooks, pass ONLY the changed row ([finalQuo]) like the Glassco 2.6 pattern instead of the whole snapshot; keep the duplicate-serial check but never re-save unrelated rows. Optionally filter the localStorage fallback in getQuotations by activeCompany().

### ⬜ P2-41 — The post-commit mirror writes new GL rows to localStorage only; FinanceService's in-memory _cache.ledger is never update
- **File:** `modules/sales/services/deliveryInvoiceService.ts:626`  ·  **module:** sales/invoicing (post-RPC local mirror)
- **Why it fails:** financeService.getLedger():510-518 returns _cache.ledger once loaded — safeSave(LS_LEDGER,...) here does not touch the cache. Repro: post an invoice, then void it (or issue a CN) in the same session: reverseDeliveryCOGS (glasscoGLDelivery.ts:346-350) looks up GL-COGS-<inv> via getLedger(), finds nothing, logs a console.warn and SKIPS the COGS reversal -> revenue reversed but COGS not, margin permanently wrong. Also any later recordTransaction -> saveLedger(_cache-based array) overwrites gtk_erp_ledger without the mirrored rows, so trial balance/AR from local data silently drop the invoice GL until the next full pull.
- **Fix:** Add a FinanceService API (e.g. appendCommittedTransactions(txs)) that pushes into _cache.ledger, updates _ledgerPushSnapshot (so they are not re-pushed) and safeSaves — and call it from the mirror block instead of raw safeSave. Alternatively await FinanceService.refresh() after the RPC.

### ⬜ P2-42 — Nippon trading COGS plan sets storeUpdates: [] — store_items.totalValue is never reduced on sale (approval only decremen
- **File:** `modules/sales/services/deliveryInvoiceService.ts:559`  ·  **module:** sales/nippon (inventory subledger)
- **Why it fails:** GL: Cr 11514 by qty x MAP at invoice. Subledger: useNipponQuotations decrements only unrestrictedQty/quantity at approval; nothing ever deducts totalValue for Nippon. After N sales, store totalValue overstates GL inventory by cumulative COGS; the stock-value <-> GL reconciliation report (P2-3) will always mismatch, and any MAP recalculation of the form totalValue/qty inflates MAP over time (qty falls, value doesn't).
- **Fix:** Populate storeUpdates in buildNipponTradingCOGSPlan ({id: si.id, deduction: qty*unitCost}) and run applyDeliveryCOGSStoreUpdates for Nippon too in the post-RPC block (it currently only fires when storeUpdates.length > 0, so wiring the array is sufficient).

### ⬜ P2-43 — The already-invoiced guard is local-cache-only, race-prone across its awaits, and does not exclude Voided invoices — dup
- **File:** `modules/sales/services/deliveryInvoiceService.ts:237`  ·  **module:** sales/invoicing (idempotency/void)
- **Why it fails:** Guard reads SalesService.getInvoices() (localStorage). (a) Fresh device / cleared cache before the invoices pull completes: guard sees nothing, allocateSerial issues a NEW number, post_invoice_atomic only rejects duplicate invoice IDs (042:134) — second invoice for the same order commits -> double revenue+COGS. (b) Two rapid submissions interleave across the awaited isTaxEnabled/allocateSerial calls with the same result. (c) Conversely a Voided invoice still matches find(i => i.orderId === order.id) -> returns alreadyInvoiced and blocks the corrective re-invoice.
- **Fix:** Server-side: in post_invoice_atomic RAISE if an invoices row exists for invoice_row->>'order_id' with status <> 'Voided' (plus partial unique index). Client-side: exclude status==='Voided' from the guard and add an in-flight module-level lock keyed by order.id.

### ⬜ P2-44 — The intercompany mirror is nondeterministic and RLS-hostile: it depends on the other company's accounts being in the loc
- **File:** `modules/sales/services/deliveryInvoiceService.ts:446`  ·  **module:** sales/invoicing (intercompany mirror)
- **Why it fails:** (1) FinanceService._loadCache loads accounts for the ACTIVE company only, so getAccounts().filter(company===target) is normally empty -> mirror silently never posts (intercompany payable missing on target books) — unless stale multi-company cache exists, when it DOES post to 'first account containing CONSUMED/MATERIAL or any Expense' — wrong accounts on another company's books. (2) Legacy fallback (line 453-458) matches substrings, so a third-party client named 'GTK Enterprises' triggers an auto-purchase in GTK's ledger. (3) post_invoice_atomic (042, no SECURITY DEFINER) inserts the mirror row with company=targetCompany; with 086-strict RLS a user whose allowed_companies lacks the target fails WITH CHECK -> the entire atomic invoice post aborts.
- **Fix:** Fetch target-company accounts explicitly from Supabase (or resolve fixed intercompany account codes per company) instead of the heuristic; drop the name-substring fallback (require explicit client.mirrorCompany); and either make post_invoice_atomic SECURITY DEFINER with an internal allowed-company check or post the mirror via a dedicated intercompany RPC so RLS cannot abort the main invoice.

### ⬜ P2-45 — GL loop skips whole detail lines when the accountId doesn't resolve (`if (!a) continue;` BEFORE glDr/glCr accumulation) 
- **File:** `modules/shared/services/dashboardMetricsService.ts:198`  ·  **module:** Dashboard metrics (Insights)
- **Why it fails:** A posted, balanced entry with one line pointing at an account missing from the cached list (JIT client account created on another device and not yet pulled, stale cache after company switch, deleted account) counts only its resolvable side → glImbalance ≠ 0 → 'GL Integrity: Off' false alarm; if BOTH sides are unresolvable a genuinely imbalanced ledger still shows 'Balanced'. The same skip silently removes amounts from cash/AR/invGL, so the Stock↔GL Tie-Out (line 331) reports 'Variance' (danger) against a correct GL or 'Reconciled' against a wrong one.
- **Fix:** Accumulate glDr/glCr for every detail line before the account lookup; only skip the classification (code prefix / type) logic when the account is unresolvable, and surface an 'unmapped postings' count.

### ⬜ P2-46 — Voided invoices are included in the 6-month revenue chart, DSO denominator, collection efficiency and top-client share.
- **File:** `modules/shared/services/dashboardMetricsService.ts:241`  ·  **module:** Dashboard metrics (Insights)
- **Why it fails:** Only liveInv (AR) excludes 'Voided' (line 231). rev90 (line 241), invoicedP (246), clientRev (269) and rev6 (274) iterate data.invoices unfiltered — void a 500K invoice via voidInvoice/090 and the dashboard revenue trend and win-metrics still count it: revenue overstated, DSO understated (larger denominator), collection efficiency understated. Reported revenue disagrees with the GL, where the RV reversal nets revenue to 0.
- **Fix:** Filter i.status !== 'Voided' (and 'Cancelled'/'Draft') in rev90, invoicedP, clientRev and the rev6 accumulation.

### ⬜ P2-47 — reverseDeliveryCOGS's inventory-value restoration is glass-only (glassAccounts().glassInv.id + category === 'Raw'), so a
- **File:** `modules\procurement\services\glasscoGLDelivery.ts:390`  ·  **module:** procurement/glasscoGLDelivery (Nippon)
- **Why it fails:** Nippon delivery COGS credits Hardware Inventory 11514 and its store items carry hardware categories, not 'Raw'. On a Nippon credit note: invCreditLine lookup by glassAccounts(company).glassInv.id finds nothing -> inventoryRelieved = 0 -> restore skipped silently (and even if it matched, store.filter(category === 'Raw') returns no Nippon rows). GL inventory account is restored by the reversing entry, but the MAP-valued store sub-ledger is not, so the stock-value <-> GL reconciliation report shows a growing gap after every Nippon CN, and MAP-based COGS on subsequent sales is understated.
- **Fix:** Branch on isTradingCompany: identify the inventory credit line by matching the ORIGINAL cogsTx detail accounts (any account whose line credited inventory) instead of hardcoding glassInv, and for Nippon restore proportionally into the specific store items the invoice consumed (persist materialDeductions/item ids in the COGS tx data blob at posting time so the reversal can target them exactly).

### ⬜ P2-48 — AsyncSalesService.getCreditNotes cloud-read OVERWRITES the unified gtk_erp_credit_notes key with only the active company
- **File:** `modules\sales\services\asyncSalesService.ts:718`  ·  **module:** sales/asyncSalesService (CN sync layer)
- **Why it fails:** persistCreditNote's push failing (offline) calls _queueRetry('credit_notes') = SyncService.markDirty; the reconnect flush rebuilds rows from 'gtk_erp_credit_notes' (SyncService.ts:105). If getCreditNotes runs first while online (cloud has >= 1 row for the company), safeSave(KEYS.CREDIT_NOTES, mapped) replaces the WHOLE unified key with the mapped cloud rows: the unsynced pending CN (and every other company's CNs) vanish from it, so the retry pushes nothing - the CN survives only in the legacy per-company key on one device and never reaches Supabase (same class as the HRService.saveEmployees markDirty/safeSave rule). A cogsReversalPending=true update queued for retry can be lost the same way, un-flagging a CN that needs finance review.
- **Fix:** Replace the safeSave overwrite with an id-keyed merge that (a) preserves rows of other companies and (b) preserves local rows not present in the cloud response (they may be unsynced): reuse _mergeIntoLocal semantics but scoped - remove only local rows of THIS company that the cloud also returned, keep everything else.

### ⬜ P2-49 — Legacy fallback has no GL-<cnId> duplicate guard before FinanceService.recordTransaction, and recordTransaction appends 
- **File:** `modules\sales\services\creditNoteService.ts:328`  ·  **module:** sales/creditNote (fallback path - live today, 090 unapplied)
- **Why it fails:** 090 is staged but unapplied, so this fallback IS the live path. handleApprove is async: two rapid clicks (or two approvers on stale local copies) both read cn.status === 'Pending Approval' from localStorage before either persistCreditNote lands, both call recordTransaction with id GL-<cnId>. recordTransaction (financeService.ts:941-949) does all.push(...) with no existing-id check, so the local ledger array holds TWO Posted entries with the same id; local trial balance/statements double the reversal until the next full cloud pull replaces the cache. reverseDeliveryCOGS has exactly this guard (line 351) - the CN reversal path forgot it.
- **Fix:** Before recordTransaction in the fallback branch: if (FinanceService.getLedger().some(t => t.id === txId)) { skip GL, still flip CN status; } - mirroring glasscoGLDelivery.ts:351. Also disable the Approve button while the async handler is in flight in CreditNoteModule.

### ⬜ P2-50 — The atomic RPC path bypasses the fiscal-period-close gate entirely: neither approveCreditNote/voidInvoice (no PeriodServ
- **File:** `modules\sales\services\creditNoteService.ts:305`  ·  **module:** sales/creditNote (period control)
- **Why it fails:** Once 090 is applied, the CN/void reversal GL is inserted by _insert_ledger_row inside the RPC with no period check. The legacy path throws 'Period X is closed' via recordTransaction (financeService.ts:932-936), so applying 090 silently REMOVES a financial control: a CN dated today against a closed prior period (or any void) posts straight into the ledger, corrupting finalized statements that Period Manager was supposed to lock.
- **Fix:** Add the client-side guard before both rpc calls: if (!PeriodService.isPeriodOpen(company, today)) throw. Defence-in-depth: add a period check inside credit_note_atomic/void_invoice_atomic (a periods table lookup or shared SQL helper) so direct RPC callers can't bypass it either.

### ⬜ P2-51 — rejectCreditNote validates 'Pending Approval' only against the LOCAL copy and then unconditionally upserts to the cloud 
- **File:** `modules\sales\services\creditNoteService.ts:410`  ·  **module:** sales/creditNote (reject path)
- **Why it fails:** User A approves CN-0001 (cloud: status Posted, GL reversal committed, invoice balance reduced). User B's device still has the local copy as Pending Approval (no realtime on credit_notes). B rejects: the local status check passes, persistCreditNote -> AsyncSalesService.saveCreditNotes upserts status='Rejected', gl_tx_id preserved-ish but status overwritten. Result: CN register says Rejected (finance believes no GL impact) while the ledger holds the posted reversal and the invoice balance stays reduced - books and register disagree, and the cogsReversalPending flag in the data blob is replaced by the stale pre-approval blob.
- **Fix:** Guard the transition in the cloud: either re-read the cloud row first (supabase.from('credit_notes').select('status').eq('id', cnId).eq('company', company)) and abort unless Pending Approval, or make reject a conditional UPDATE ... .eq('status','Pending Approval') and treat 0 rows as a conflict; ideally a tiny reject_credit_note RPC mirroring the approve guard.

### ⬜ P2-52 — Ledger pull never surfaces reqId from the data blob (and the push column list omits data/req_id) — after any sync pull, 
- **File:** `src/services/SyncService.ts:189`  ·  **module:** Sync (ledger hybrid mapping)
- **Why it fails:** The live ledger keeps reqId only inside the partial jsonb blob (data={reqId}, written by ledgerToRow financeService.ts:285). pullTable has no TABLE_PULL.ledger entry, so mapFromSupabase (line 1625) leaves reqId buried in row.data and overwrites gtk_erp_ledger with rows lacking tx.reqId; TABLE_COLUMNS.ledger (line 189) also drops data/req_id on pushTable inserts. Consumers of getLedger() then lose the linkage: postParkedPV (financeService.ts:1483) skips the requisition→'Paid' update and getOutstandingAdvances (1346) can't match SETTLE- rows, so paid requisitions and settled advances show outstanding forever on those screens.
- **Fix:** Add a TABLE_PULL.ledger mapper that reuses rowToLedger (surfaces r.data?.reqId → reqId), and append 'data' to TABLE_COLUMNS.ledger so pushTable inserts don't strip the blob.

### ⬜ P2-53 — Requisitions have two divergent cloud writers and a pull mapper that reads flat columns only: receiptStatus/paymentStatu
- **File:** `src/services/SyncService.ts:1056`  ·  **module:** procurement/requisitions sync (hybrid mapping)
- **Why it fails:** InventoryService.saveRequisitions (inventoryService.ts:611) upserts {id, company, data: fullObject}; SyncService toRowMappers.requisitions (line 280) pushes only 12 flat fields (no receipt_status/payment_ref/sub_category); fromRowMappers.requisitions (line 1056) spreads the row and maps flat columns but never unwraps r.data. GTKStoreReceipt.tsx:380 marks receiptStatus='Received' via saveRequisitions (data blob only, no markDirty). Next fetchFromCloud overwrites gtk_erp_requisitions with objects lacking receiptStatus -> the same approved requisition shows as un-received, can be GRN'd and advance-settled a second time (settleAdvance finds the same [ADVANCE] entry again).
- **Fix:** Single source of truth per table: make fromRowMappers.requisitions merge r.data over the flat columns ({...r, ...(r.data||{}), headerText: ...}), and add the missing fields (receipt_status, payment_status, payment_ref, sub_category, payment_mode) to toRowMappers so both push paths agree. Add a settled/received idempotency guard in settleAdvance (reject if a SETTLE-* tx for the reqId already exists).

---

## P3 (11)

### ⬜ P3-54 — Default 'All Receivable Accounts' view aggregates every level-5 Asset account — cash, banks, inventory and advances coun
- **File:** `modules/finance/components/AgingReport.tsx:63`  ·  **module:** AR Aging (components, orphaned)
- **Why it fails:** targetAccounts filters only level===5 && type==='Asset' when no control account is selected, so 11112 Cash in Hand, 1112x banks, 115xx stock and 11421 employee advances each render as 'receivable' rows with FIFO buckets, and the headline exposure ≈ total current assets. Same for Payables (all L5 liabilities incl. GST/salaries payable). Currently unmounted in the multitenant app, but it is the designated FIFO aging component and has a workaround (pick a control account) — flag before re-wire.
- **Fix:** Default the account set to the AR control subtree (parentId chain under code 122/1221; AP under 2111) instead of all L5 accounts of the type.

### ⬜ P3-55 — A voucher can be Posted with detail lines that have amounts but no G/L account selected — the amounts then vanish from e
- **File:** `modules/finance/pages/GeneralLedger.tsx:175`  ·  **module:** GeneralLedger (entry validation)
- **Why it fails:** isBalanced (line 173) only checks Σdebit===Σcredit>0 and _assertGLBalance does the same; accountId:'' lines pass. Post Dr 100k on a real account / Cr 100k on '-- Choose G/L --': the trial_balance RPC excludes ''-account lines (088:66), buildRows drops them (no matching account), dashboardMetrics skips them — TB and statements show a one-sided 100k with a permanent 'ledger imbalance' warning while the stored entry claims to balance.
- **Fix:** In handleSaveDocument (and financeService.saveLedger for Posted entries), reject any detail line where (debit>0||credit>0) && !accountId.

### ⬜ P3-56 — Balance Sheet and Trial Balance are computed from a from..to doc_date window — cumulative position reports become period
- **File:** `modules/finance/pages/ReportsHub.tsx:97`  ·  **module:** Finance Reports (ReportsHub — TB/BS window)
- **Why it fails:** loadReportData applies .gte('doc_date', from) to the ledger for ALL reports; the Balance Sheet 'as of' toDate then excludes every posting before fromDate (default Jan-1 of the current year; the 'FY 2025' quick pick shows a 2025-movement-only 'balance sheet'). Opening balances/prior-year AR, cash and equity vanish, and the isBalanced badge (line 387) reflects the window, not the books. Currently mostly masked because history starts in 2026, but it breaks on the first year rollover.
- **Fix:** For balance_sheet (and TB 'as of'), load the ledger with only .lte('doc_date', to) (no gte); keep the from..to window for P&L/Cash-Flow only.

### ⬜ P3-57 — P&L wraps period group totals in Math.abs — a window where credit notes exceed sales shows the net revenue REVERSAL as p
- **File:** `modules/finance/pages/ReportsHub.tsx:264`  ·  **module:** Finance Reports (ReportsHub — P&L sign)
- **Why it fails:** revenue = Math.abs(getGroupTotal('Revenue')): pick a from..to containing only a 200k credit-note reversal (Dr revenue) → Revenue displays +200k and netProfit is overstated by 400k versus reality. Same pattern in FinancialStatements.tsx:32 and FinancialStatementsMobile.tsx:119. Sign-sensitive in exactly the windows where management inspects returns.
- **Fix:** Compute revenue as (credit − debit) and expenses as (debit − credit) without abs; display negative results as negative (bracketed) figures.

### ⬜ P3-58 — Thickness matching by name.includes(mm) mis-attributes materials ('6' matches '16mm' names; fallback = first raw item), 
- **File:** `modules/procurement/services/glasscoGLService.ts:143`  ·  **module:** procurement/glasscoGL (material matching)
- **Why it fails:** Store has '16mm Clear' listed before '6mm Clear': a 6mm piece matches '16mm Clear' first (name includes '6') in both postTemperingInwardGL (line 143-149) and getPieceCostData (glasscoGLHelpers.ts:155) -> tempering cost allocated to 16mm stock's MAP and delivery COGS priced at 16mm MAP; when nothing matches, store[0] silently absorbs the cost. Wrong per-material valuation, wrong COGS amount.
- **Fix:** Match with a word-boundary/regex on the numeric prefix (e.g. /(^|[^0-9])6(\.0)?mm/) or store a structured thicknessMm field on store items and compare numerically; log + skip instead of falling back to store[0].

### ⬜ P3-59 — assertSufficientStock and assertPOBudget are never called anywhere; GoodsIssue instead checks a stale component-prop cop
- **File:** `modules/procurement/services/inventoryService.ts:268`  ·  **module:** inventory/stock gates (dead code)
- **Why it fails:** Grep shows both functions have zero call sites. GoodsIssue.tsx:54 validates against the `items` prop loaded at page mount — if another tab/session issued stock since, the check passes and saveStore writes a resurrected stale quantity (oversell for Glassco/GTK slips through until the saveStore negative guard, which only catches it after the fact and not for partial oversell). assertPOBudget (line 308) builds lt('date', `${fiscalYear}-13-01`) for fiscalMonth=12 — invalid date -> query error -> poErr ignored -> committed=0 -> budget gate silently passes.
- **Fix:** Wire assertSufficientStock into GoodsIssue/dispatch/transfer paths (await the live-DB check before decrementing), wire assertPOBudget into the PO approval action, fix the month rollover (year+1, month 1 when fiscalMonth===12), and fail-closed (or at least warn) when the committed-PO query errors.

### ⬜ P3-60 — manualSerial uniqueness is a read-then-write check with no DB unique constraint — two devices (or offline + online) can 
- **File:** `modules/sales/companies/nippon/useNipponQuotations.ts:332`  ·  **module:** sales/nippon
- **Why it fails:** Device A saves serial 0012 offline (push queued); device B online checks getQuotations (A's row not in cloud yet) → isDuplicate false → saves QT-0726-0012. When A's queued push lands, upsert onConflict:'id' overwrites B's quote (or vice-versa) with no error, and the SO/print history now shows the wrong client/items under that serial.
- **Fix:** Add a partial unique index (company, manual_serial) WHERE manual_serial IS NOT NULL on quotations, surface 23505 as a 'serial already used' toast, and include a random suffix check on id collision before upserting.

### ⬜ P3-61 — Generic editor edits fields the pricing engine ignores — the user-typed Rate ('rate') and thickness ('glassThickness') n
- **File:** `modules/sales/pages/QuotationManager.tsx:228`  ·  **module:** sales/generic (GTK/GTI)
- **Why it fails:** The Rate input writes item.rate and the thickness select writes item.glassThickness, but useQuotations.updateGlassItem reprices only on ['glassSize','glassType','selectedServices'] and computes amount = totalSqFt × pricePerUnit (useQuotations.ts:98-125). Entering W/H/qty plus a manual Rate of 500 leaves pricePerUnit at the auto-rate (often 0 for GTK, which has no Glass products) → line amount 0 while the grid shows Rate 500; the quote saves and prints with the wrong (zero) financial total.
- **Fix:** Bind the editor inputs to the real fields (pricePerUnit, glassSize) or map rate→pricePerUnit / glassThickness→glassSize inside updateGlassItem before the amount recalculation; block GTK/GTI go-live on this screen until aligned.

### ⬜ P3-62 — Maker-checker identity is a client-supplied display string (fullName || email) compared client-side only; 090's RPC neve
- **File:** `modules\finance\components\CreditNoteModule.tsx:30`  ·  **module:** sales/creditNote (maker-checker)
- **Why it fails:** actor = user?.fullName || user?.email: two different finance users both named 'Hassan' are wrongly blocked from approving each other's CNs (createdBy === approver compares equal); conversely the maker can edit their profile fullName (or call approveCreditNote/the RPC directly from the console) and approve their own CN - credit_note_atomic (090:75-84) checks only the status, never cn_data->>'createdBy' vs the authenticated caller. The JV maker-checker path this claims to mirror compares emails and roles.
- **Fix:** Use a stable identity (auth user id or email from useAuthStore, never fullName) for createdBy/approvedBy, and enforce inside credit_note_atomic: IF v_cn_data->>'createdBy' = (SELECT email FROM user_profiles WHERE id = auth.uid()) THEN RAISE 'maker_checker_violation'.

### ⬜ P3-63 — voidInvoice's legacy fallback reverts the quotation via sync LOCAL-only SalesService.getQuotations/saveQuotations - on a
- **File:** `modules\sales\services\creditNoteService.ts:509`  ·  **module:** sales/creditNote (void fallback, quotation revert)
- **Why it fails:** The fallback (live today, 090 unapplied) mixes sources: invoices go through AsyncSalesService (cloud read+write) but quotations through SalesService.getQuotations() = safeParse local. On a device whose quotations cache is empty/missing that quote, allQ.map matches nothing, saveQuotations pushes an array without the quote - cloud quotation keeps status 'Invoiced' + invoiceNo pointing at a Voided invoice, so the order can never be re-invoiced (generateDeliveryInvoice's already-invoiced/status gates trip) and reporting counts a live invoice link to a voided document.
- **Fix:** In the fallback, update the quotation through AsyncSalesService (fetch by id, patch status='Approved' + clear invoiceNo, save) exactly as the invoice is handled two lines above, or do a targeted supabase.from('quotations').update({status:'Approved', data: ...}).eq('id', quotationId).eq('company', company).

### ⬜ P3-64 — Both RPCs lock/UPDATE invoices and quotations by id alone with no company predicate - for super-admin sessions (auth_use
- **File:** `supabase\migrations\090_credit_note_void_atomic.sql:174`  ·  **module:** migrations/090
- **Why it fails:** credit_note_atomic (UPDATE invoices WHERE id = v_inv_id, 090:99-103) and void_invoice_atomic (SELECT ... FOR UPDATE WHERE id = v_inv_id at 174; UPDATE quotations WHERE id = v_quote_id at 213-217) accept v_company but never use it as a row filter. For normal users 086 RLS makes a cross-company id match 0 rows (safe), but a super admin - the go-live daily driver - bypasses RLS, so a bug like the appStore.selectedCompany changing between UI load and click (payload company = Nippon, invoice loaded from Glassco) silently voids/rebalances the other company's document.
- **Fix:** Add AND company = v_company to every invoices/quotations/ledger/credit_notes predicate in both RPCs (and RAISE company_mismatch when the locked row's company <> v_company) before 090 is applied - it is still unapplied, so this is a file edit, not a new migration.

---

