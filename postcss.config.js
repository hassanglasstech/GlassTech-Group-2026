// Auto-detected by Vite. Runs Tailwind (build-time) + Autoprefixer over the CSS
// entry (index.css, imported in index.tsx). Replaces the runtime Tailwind CDN.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
